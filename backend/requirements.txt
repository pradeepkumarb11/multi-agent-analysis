# ============================================================
# Multi-Agent LLM Data Analysis System — Python Dependencies
# Pin minor versions for reproducible deploys on Render
# ============================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20          # multipart/form-data (CSV upload)

# --- Database ---
supabase==2.10.0                  # Supabase Python client

# --- Task queue + Redis ---
arq==0.26.1                       # ARQ async task queue
redis==5.2.1                      # Redis client (used by ARQ + pub/sub)

# --- LLM + Agent framework ---
langchain==0.3.14
langchain-groq==0.2.3             # Groq LLM integration
langgraph==0.2.67                 # LangGraph agent orchestration
langchain-core==0.3.29

# --- Data + charting ---
pandas==2.2.3
matplotlib==3.10.0
numpy==2.2.1

# --- Utilities ---
python-dotenv==1.0.1              # Load .env locally
pydantic==2.10.5                  # Structured LLM outputs
httpx==0.28.1                     # Async HTTP (used internally by supabase)
tabulate==0.9.0                   # Eval results table (eval.py)
aiofiles==24.1.0                  # Async file ops
